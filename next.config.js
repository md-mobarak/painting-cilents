/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.freepik.com'], // Add the hostname(s) of your image source(s) here
  },
}

module.exports = nextConfig;